#include "main.h"
/**
* _abs - computes the absolute value of an integer
* @n: interger argument
* Return: absolute value of the interger
*/

int _abs(int n)
{
if (n > 0)
return (n);
else if (n < 0)
return (-n);
return (0);
}
